/* this is multi-line comment */
//this is single line comment
//hallo
#include <stdio.h>
#include <string.h>
#include     "string.h"
#define check 10
int main()
{
    char Z = 'S';
    char str[2];
    int i;
    int sum=10;
    float ft = 10.98;
    char name = 'a';
    printf("hello world");
    printf("hallo %d -> sum",sum);
    printf("%c",name);
    printf("%.1f",ft);
    if(sum==1)
    {
        return 0;
    }
    else if(sum==0)
    {
        return 1;
    }
    do
    {
        /* code */
    } while (1);
    for(i=0;i<=sum;i++)
    {
        sum++;
    }
    return 0;
}